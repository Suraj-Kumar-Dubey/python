import sys
class Database:
	usrname="root"
	Pass="root"
	username=sys.argv[1]
	password=sys.argv[2]
	if (usrname==username && Pass==password):
		if Pass==password:
			if query==exit:
				exit
			elif query==create:
				
			elif query==insert:
				
			else:
				
		else:
			print "enter valid password"
			exit
	else:
		print "Enter valid username"
		exit
